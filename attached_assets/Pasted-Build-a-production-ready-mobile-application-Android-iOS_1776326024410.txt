Build a production-ready mobile application (Android + iOS) based on the provided web app UI and flows (Aura Pendant Pro). The app must NOT be a simple WebView wrapper — it should be a fully functional hybrid/native-grade app using React Native (preferred) or Capacitor with deep native integrations.

## 🎯 Core Goal

Recreate the UI/UX exactly as shown, but upgrade it into a high-performance mobile app with real device integrations, offline support, and scalable architecture.

---

## 🧱 Tech Stack Requirements

* React Native (Expo or CLI) OR Capacitor (if hybrid)
* TypeScript (strict mode)
* State management: Zustand or Redux Toolkit
* Navigation: React Navigation (bottom tabs + stack)
* UI: Tailwind (NativeWind) OR custom design system
* Backend-ready: API layer abstraction (REST/GraphQL)

---

## 📱 Core Screens & Features

### 1. Home Dashboard

* Device connection status (Connected / Disconnected)
* Battery indicator with real-time updates
* Storage usage (progress bar)
* Media stats (Photos, Videos, Today)
* Health widget (heart rate graph, min/avg/max)

👉 Add:

* Real-time sync via WebSocket or BLE
* Smooth animated charts (Reanimated + Skia)

---

### 2. Live Camera

* Start live preview from device
* Capture photo / video
* Switch modes (Photo / Video)
* Flash toggle, audio recording toggle

👉 Native integrations required:

* Camera API (not web camera)
* Background recording support
* Save to device + cloud sync

---

### 3. Gallery

* Tabs: All / Photos / Videos / Starred
* Grid with lazy loading
* Storage indicator

👉 Add:

* Offline cache (view media without internet)
* Smart grouping (date/location)
* AI auto-tagging (faces, events)

---

### 4. Alerts System

* Categories: SOS / Health / Device
* Real-time notifications
* Dismiss / filter alerts

👉 Add:

* Push notifications (Firebase/APNs)
* Critical alerts bypass silent mode
* Alert history timeline

---

### 5. SOS & Contacts (Important Feature)

* Add/remove emergency contacts
* Toggle SOS alerts per contact
* Call / message directly

👉 Hidden features to implement:

* One-tap SOS trigger
* Background GPS tracking during SOS
* Auto-send live location + camera stream
* Silent SOS mode (no UI indication)

---

### 6. Settings Screen

* Device info (firmware, storage)
* Camera settings (resolution, flash, night mode)
* Privacy (encryption, biometric lock)

👉 Add:

* Biometric authentication (FaceID/Fingerprint)
* Secure local storage (encrypted)
* Remote device wipe

---

## 🔗 Device Integration (CRITICAL)

Simulate or implement:

* BLE (Bluetooth Low Energy) connection to pendant
* Battery + storage sync
* Camera streaming from external device

If real hardware is not available:

* Mock device layer with realistic data + states

---

## 🧠 Hidden / Advanced Features (Must Include)

### 1. Background Services

* Run SOS tracking in background
* Continue recording even if app is closed

### 2. Smart Notifications

* Low battery alerts
* Device disconnect alerts
* Daily health summary

### 3. Offline Mode

* Cache last known data
* Queue actions (sync later)

### 4. Security Layer

* End-to-end encryption toggle
* Biometric app lock
* Secure API calls with token refresh

### 5. Performance Optimization

* Lazy loading everywhere
* Image compression
* Memoized components

---

## 🎨 UI Requirements

* Match the exact dark UI theme from screenshots
* Glassmorphism + gradient accents
* Smooth transitions (Reanimated)
* Bottom tab bar with glow effect

---

## 📦 App Structure

/src
/components
/screens
/navigation
/store
/services (API, BLE, camera)
/hooks
/utils

---

## 🔌 API Layer

* Abstract all API calls
* Prepare for:

  * Device data
  * Media upload
  * User auth
  * Notifications

---

## 🚀 Deliverables

* Fully working mobile app
* Clean, modular codebase
* Ready for Play Store & App Store
* Mock backend if needed

---

## ⚠️ Constraints

* Do NOT use WebView as main UI
* Do NOT skip native features (camera, notifications, storage)
* Code must be scalable and production-ready

---

## 🧩 Bonus (If Possible)

* Add wearable/watch support
* Add voice-triggered SOS
* Add AI health insights (basic)

---

Use the provided UI images as the exact design reference. Maintain pixel-perfect consistency but improve usability where needed for mobile ergonomics.
